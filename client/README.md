# MERN + OAuth Image Search (Unsplash)

## Quick start

1. Clone repo.
2. Create `/server/.env` from `/server/.env.example` and fill values (MONGO_URI, SESSION_SECRET, UNSPLASH_ACCESS_KEY, OAuth creds).
3. Start backend:
   ```bash
   cd server
   npm install
   npm run dev
